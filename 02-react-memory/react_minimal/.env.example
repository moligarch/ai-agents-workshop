# Copy to .env and fill values
OPENAI_API_KEY=sk-...

# Optional model override (depends on your router/provider)
MODEL=gpt-4o-mini

# Optional: point the OpenAI SDK to a router/proxy
# Example (MetisAI):
# OPENAI_BASE_URL=https://api.metisai.ir/openai/v1
# You can also use OPENAI_API_BASE if your environment prefers that name.